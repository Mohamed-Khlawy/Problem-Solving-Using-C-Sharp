namespace H._Simple_Mod
{
    internal class Program
    {
        static void Main(string[] args)
        {
            string number = Console.ReadLine()!; // There is no need for it 
            // Because 0 mod any number will be zero, so no need for using the user number

            Console.WriteLine(0 + " " + 0);
        }
    }
}